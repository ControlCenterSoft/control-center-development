name: ServerOps staging bridge preflight

on:
  push:
    branches:
      - ops/serverops-staging-secret-preflight
    paths:
      - .github/workflows/serverops-staging-secret-preflight.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  private-staging-preflight:
    uses: ControlCenterSoft/control-center/.github/workflows/serverops-reusable-preflight.yml@ops/serverops-reusable-bootstrap
